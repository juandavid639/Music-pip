{
  "name": "music-pip",
  "version": "1.0.1",
  "private": true,
  "description": "Pruebas y utilidades de desarrollo. La extension NO se compila: se carga tal cual desde la raiz del proyecto.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "empaquetar": "powershell -ExecutionPolicy Bypass -File tools/empaquetar.ps1"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
